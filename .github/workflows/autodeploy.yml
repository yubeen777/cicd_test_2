name: GithubAction 자동 배포

on:
  push:
    branches:
      - main

jobs:
  My-Auto-CICD-job:
    runs-on: ubuntu-latest

    steps:
      - name: test
        run: echo "test"

      - name: test
        run: |
          pwd
          ls -al
